import { IssuerRouteTypes, LoginOptions } from '../types';
/**
 *
 * @param options
 * @param type
 * @returns URL to redirect to
 */
export declare const generateAuthUrl: (domain: string, type: IssuerRouteTypes | undefined, options: LoginOptions) => Promise<{
    url: URL;
    state: string;
    nonce: string;
    codeChallenge: string;
}>;
export declare function generatePKCEPair(): Promise<{
    codeVerifier: string;
    codeChallenge: string;
}>;
