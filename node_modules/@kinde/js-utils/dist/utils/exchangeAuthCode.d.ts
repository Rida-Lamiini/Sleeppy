import { StorageKeys } from '../main';
export declare const frameworkSettings: {
    framework: string;
    frameworkVersion: string;
};
interface ExchangeAuthCodeParams {
    urlParams: URLSearchParams;
    domain: string;
    clientId: string;
    redirectURL: string;
    autoRefresh?: boolean;
}
interface ExchangeAuthCodeResult {
    success: boolean;
    error?: string;
    [StorageKeys.accessToken]?: string;
    [StorageKeys.idToken]?: string;
    [StorageKeys.refreshToken]?: string;
}
export declare const exchangeAuthCode: ({ urlParams, domain, clientId, redirectURL, autoRefresh, }: ExchangeAuthCodeParams) => Promise<ExchangeAuthCodeResult>;
export {};
