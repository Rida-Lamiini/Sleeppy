import { JWTDecoded } from '@kinde/jwt-decoder';
/**
 * get all claims from the token
 * @returns { Promise<T | null> }
 */
export declare const getClaims: <T = JWTDecoded>() => Promise<T | null>;
