import { JWTDecoded } from '@kinde/jwt-decoder';
/**
 *
 * @param tokenType Type of token to decode
 * @returns { Promise<JWTDecoded | null> }
 */
export declare const getDecodedToken: <T = JWTDecoded & {
    permissions: string[];
    org_code: string;
}>(tokenType?: "accessToken" | "idToken") => Promise<T | null>;
