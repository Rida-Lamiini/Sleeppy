import { StorageKeys } from '../../sessionManager';
interface RefreshTokenResult {
    success: boolean;
    error?: string;
    [StorageKeys.accessToken]?: string;
    [StorageKeys.idToken]?: string;
    [StorageKeys.refreshToken]?: string;
}
/**
 * refreshes the token
 * @returns { Promise<boolean> }
 */
export declare const refreshToken: (domain: string, clientId: string) => Promise<RefreshTokenResult>;
export {};
