/**
 *
 * @param keyName key to get from the token
 * @returns { Promise<string | number | string[] | null> }
 */
export declare const getFlag: <T = string | number | boolean>(name: string) => Promise<T | null>;
